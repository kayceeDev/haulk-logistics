const inventoryModel = require("./inventory");
const shipmentModel = require("./shipment");
const categoryModel = require("./category");
const statusModel = require("./status_history");

module.exports = {
  inventoryModel,
  shipmentModel,
  categoryModel,
  statusModel,
};
